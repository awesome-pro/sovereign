export interface Deal {
    id: string
    referenceNumber: string
    title: string
    description: string
    createdAt: string
    updatedAt: string
}


export interface RelatedDeal {
    id: string
    referenceNumber: string
    title: string
}